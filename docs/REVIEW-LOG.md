# REVIEW-LOG — 审查记账

> 双 agent 审查的长期沉淀。流程见 [WORKFLOW.md](WORKFLOW.md)。
> 最后更新：2026-09-11

**这份文件的价值全在于「不重复」**：同一个问题被指出两次、同一个争论吵两回，就说明该写进这里而没写。

三类东西往这里记：

1. **反复出现的问题** —— 同一类缺陷被指出 ≥2 次，说明是习惯性错误，要固化成约定
2. **已达成的约定** —— 争论之后定下来的做法，以后直接引用，不再重新论证
3. **被驳回的意见及理由** —— 审查提了但决定不改的，写清为什么，避免下次又提

---

## 反复出现的问题

| 日期 | 来源 | 问题 | 出现次数 | 固化成什么 |
| --- | --- | --- | --- | --- |
| 2026-09-10 | #2 | **写流程/约定时没有回头检查它和既有文档是否冲突**。一次就撞出三处：闭环步骤 vs TODO 约定、「只读」vs 写临时文件、公开仓库 vs spec §99 | 1（三处同源） | 新增或修改任何约定前，先 grep 既有文档里同一主题的表述，冲突就挑明并收口，不要新写一条了事 |
| 2026-09-10 | #4 | **把两个同名但不同层的概念混成一个**。ADR-0004 把「签名密钥版本」（`key_version`，随请求头传给客户端）和「加密封装版本」（内部的、客户端不可见）写进同一张轮换表 | 1 | 一个字段被两层用到时，先问「客户端看得见吗」。看得见的和看不见的**必须分表说明** |
| 2026-09-10 | #4 | **数值推导没走最坏情况**。TTL 定 6 分钟，漏了时间窗是 **±**5 分钟——带未来时间戳的请求有效期跨两个窗口，留下约 4 分钟重放洞 | 1 | 凡是「窗口 / 超时 / TTL」类数值，必须画出最坏情况时间线再定，不能凭直觉取「窗口 + 一点余量」 |
| 2026-09-10 | #4 | **论据里的算术错误**。ADR-0003 写「T+3 也挡不住 31 小时积压」——T+3 是 72 小时，覆盖得住 | 1 | 写进 ADR「备选方案」的每条否定理由都要能算得出来；错误论据比没有论据更糟，它会让后来者以为问题已经论证过 |
| 2026-09-10 | #5 | **同一个判断在写入方和读取方各写了一份实现**。发布审查评论时只校验最后一行的判定，读取批准记录时却还要求首行是署名前缀——脚本会发布一条自己认为合法、读取方判为「不是审查」的批准，两端各说各话且都不报错 | 1（属下面「改了 A 忘了改 B」同一族） | 一个格式约定只准有一份实现。写入方必须调用读取方那个函数，不许各写各的校验 |
| 2026-09-10 | #5 | **专项检查的覆盖面靠「想得到」**。审查后的基线复检逐项加：工作区 → PR head → 设计版本 → 批准状态，每轮都被指出还漏一项（本地 `HEAD` 切换、设计正文原地改写、PR 改关联另一个设计） | 3（连续三轮） | 能整体比对就不要逐项列举。审查材料改成重新取一次**逐段比对**，专项检查只保留给出具体错误信息的职责 |
| 2026-09-10 | #5 #6 | **改了 A 处忘了改引用同一结论的 B 处** —— 本仓库最顽固的一类。已发生：设计闸门分档文本、PR 模板缺 `设计闸门：#N`、重写文件丢 UTF-8 BOM、ADR-0004 改了 nonce 过期而 TODO 的 R4 仍写 6 分钟、ADR-0003 推翻「上期调整是常态」而 TODO 的 D5/R7 仍是旧结论、ADR-0004 把出站 schema 列为未决而 TODO 的 D4 仍标「已收口」 | **6** | 改一个结论时，先 `rg` 出所有复述该结论的地方一起改。**TODO 是实现者真正会照着做的文件**，它与 ADR 不一致 = 缺陷会被原样引回来。能机械化的已进 `check_docs.py` 的约定串规则 |
| 2026-09-10 | #6 | **声称「已并入 / 已写进」但实际没改**。ADR 写「网关幂等标识已并入 D6」而 D6 没这条；PR 描述写「主密钥属主已写进收口条件」而九条收口条件里没有 | **2** | 写「已并入 X」之前先去 X 那里改完再回来写。**这类假引用比遗漏更糟**——它让人以为已经追踪了，于是没人再去查 |
| 2026-09-10 | #6 | **写流程时没有对照表结构验证可行性**。ADR-0004 的密钥轮换重叠期要求新旧并存，但 `projects` 只有一组 webhook 密钥槽，流程在现有 schema 上无法实现——而同一份 ADR 还声明「表结构不因本 ADR 而变」 | 1 | 凡是写「流程」，都要指着**具体表的具体字段**问一遍「这一步存在哪里」。存不下就是设计没完成 |
| 2026-09-10 | #6 | **把配置写法当常识，没查文档**。主密钥文件写 `root:root 0400`，但 Docker Compose 的 `file:` secret 走 bind mount，`uid`/`gid`/`mode` 只在 swarm 模式生效——非 root 容器直接读不到，服务起不来 | 1 | 涉及具体工具行为（权限、挂载、默认值）的断言，要么查文档，要么标注「未验证」。**不要凭印象写进 ADR** |
| 2026-09-10 | #6 | **两条要求互斥却都写进了文档**。ADR-0004 说主密钥 runbook「不进本仓库」，spec §136 要求仓库内 `docs/runbook.md` 覆盖 master-key recovery | 1 | 遇到「安全要求」与「文档要求」冲突，不能只满足一边。要**定义边界**：脱敏流程进仓库，具体值进私有附录 |
| 2026-09-10 | #6 | **合并评估两个不同量级的备选方案**。ADR-0003 把 T+3 与 T+7 合在一起，用 T+7 的代价（客户等到 8 号）否定了 T+3（实际第 4 日出单） | 1 | 备选方案逐个独立评估，各自算出具体后果。**合并否定会让一个本来可行的方案被误杀** |
| 2026-09-10 | #6 | **把告警示例当成常态依据**。用 §31 的「31 小时积压」告警示例论证「上期调整是常态」，进而要求 UI 与客服按常态设计——这等于把故障信号正常化 | 1 | 引用 spec 里的数字前先看它的**语境**：告警阈值、示例、目标值、实测值是四种不同性质的东西 |
| 2026-09-11 | #8 | **跨层假设**。含正则的内容经 Python heredoc → PowerShell 传递，`-split "\r?\n"` 落成字面回车换行，分割静默失效；bash 的 `/tmp` 传给 Windows Python 变成 `\tmp`，而 `gh` 用的是 bash 的路径——报成功、实际把原文写了回去；本机 heredoc 还会把 `\\` 压成 `\` | 5 | 含正则 / 转义 / 反斜杠的内容只走 Write / Edit 工具，不进 shell；GitHub 写操作一律经 `gh_verified_write.py` 回读比对 |
| 2026-09-11 | #5 | **审查中的 PR 里继续加东西**。#5 每轮修完顺手加新检查、顺手修旁边的过期引用，新加的面积下一轮就被抓；六轮不收敛的直接成因 | 贯穿 #5 六轮 | 复审材料自动带增量 diff，与修复无关的改动判为阻断；第三轮起强制写「完整影响面」 |

> **这张表里没有一条是自查发现的**，全部来自独立审查。它们也全都不在 `check_docs.py` 的覆盖范围内——自动检查能查死链和 `§N` 是否存在，查不出两处表述在**语义**上互斥。这正是独立审查不可替代的地方。

---

## 已达成的约定

| 日期 | 约定 | 起因 | 来源 |
| --- | --- | --- | --- |
| 2026-09-10 | 审查者只读不写：Codex 不改代码、不 push、不合并 | 一旦审查者动手改，就变成第二个开发者，独立性消失 | [WORKFLOW.md](WORKFLOW.md) §1 |
| 2026-09-10 | 评论必须带 `## 🔍 CODEX REVIEW` / `## 🔧 CLAUDE RESPONSE` 前缀 | 两边共用同一个 GitHub 账号，否则分不清发言人 | [WORKFLOW.md](WORKFLOW.md) §6 |
| 2026-09-10 | `VERDICT:` 必须是审查评论的最后一行，取值只有两种 | 需要机器可读地判断是否还要再走一轮 | [WORKFLOW.md](WORKFLOW.md) §6 |
| 2026-09-10 | Claude 不许沉默跳过任何审查意见，只能改 / 说明理由 / 升级 | 沉默跳过会让审查形同虚设 | [WORKFLOW.md](WORKFLOW.md) §10 |
| 2026-09-10 | spec 是唯一事实来源，导航层文档不得另立说法 | docs/ 是索引层，两处定义会漂移 | [ARCHITECTURE.md](ARCHITECTURE.md) 开头 |
| 2026-09-10 | 任务记录与审查教训必须写在分支上，随 PR 一起进 `main` | `main` 受保护不能直推，"合并后补记录"要另开 PR，是闭环外的动作 | [WORKFLOW.md](WORKFLOW.md) §2 |
| 2026-09-10 | 审查者「只读」的唯一例外是 `.codex-review-<N>.md` 临时文件 | 不写这个例外，严格遵守「不修改任何文件」的审查者就无法生成 `--body-file` 的输入 | [WORKFLOW.md](WORKFLOW.md) §1 |
| 2026-09-10 | 与 spec 冲突的既成事实必须显式记为「偏离」并给出收口选项 | 不记录的话，后来者无法分辨那是已批准的取舍还是失误 | [ARCHITECTURE.md](ARCHITECTURE.md) §7.1 |
| 2026-09-10 | 评论格式的校验只有一份实现，发布方直接调用读取方的函数 | 各写一份会发布「自己合法、对方判为非审查」的批准，闸门两端各说各话 | [WORKFLOW.md](WORKFLOW.md) §6 |
| 2026-09-10 | 判定发布前重新取一次审查材料**逐段比对**，逐字不变才发布 | 逐项列举专项检查漏了三轮；整体比对不依赖「想得到」 | [WORKFLOW.md](WORKFLOW.md) §6 |
| 2026-09-11 | 回应表里「已修」的证据必须是 `完整 SHA · 文件:行`，机器校验其在所指提交上存在 | 「声称已写进 X，实际没写」出现 3 次；要填行号就得打开文件看一眼 | [WORKFLOW.md](WORKFLOW.md) §6 |
| 2026-09-11 | 每个 PR 必须显式声明 `TODO 影响`（updated + 行号 / none + 理由），与 diff 是否碰 `docs/TODO.md` 交叉校验 | 「改了 ADR 没改 TODO」出现 6 次 | [WORKFLOW.md](WORKFLOW.md) §4、PR 模板 |
| 2026-09-11 | 发到 GitHub 的写操作必须回读比对，工具报成功不算数；回读失败不自动重发 | `gh pr edit` 报成功却写回原文 | [WORKFLOW.md](WORKFLOW.md) §6 |
| 2026-09-11 | CI 全绿 + PR 正文过策略检查，才调 Codex；复审材料带上轮审查、回应与增量 diff；第三轮起必须写「完整影响面」 | 配额不花在注定被打回的材料上；连续两轮「还漏一项」= 该停止逐项补洞 | [WORKFLOW.md](WORKFLOW.md) §4 |
| 2026-09-11 | 复选框只允许 `[ ]` / `[x]`；约定不够用时不发明新状态 | `[~]` 让扫待办的人整个跳过那条 | `check_repo_policy.py` |
| 2026-09-11 | 设计闸门分档看**主题**，不看「是不是 API」；新增端点本身不构成走闸门的理由 | 分档表原本只有三行，「既不碰钱、也不碰用量摄取 / 集成认证 / Webhook 的后端改动」无处可归，#22 就在这个空档上吵了一轮 | [WORKFLOW.md](WORKFLOW.md) §3 |

---

## 机械化对照 —— 哪些已经交给机器，哪些仍靠人

| 反复出现的问题 | 机器检查 | 回归测试 | 机器抓不住、仍靠语义审查的部分 |
| --- | --- | --- | --- |
| 改了 A 忘了改 B（6 次） | `check_docs.py` 约定串规则（字面串）；`check_repo_policy.py` 的 `TODO 影响` 声明 × diff 交叉校验 | `test_check_repo_policy.py` PrBodyTests | ADR 与 TODO **内容**上是否一致——「上期调整是常态」这种没有共同字符串 |
| 声称做了实际没做（3 次） | 「已修」证据 `SHA · 文件:行` 必须在所指提交上存在 | ResponseTests | 那一行是否**真的**解决了指出的后果 |
| 自己发明约定（`[~]`） | 复选框白名单 | CheckboxTests | 其它形式的「自造状态」（新的措辞、新的标签） |
| 跨层假设（5 次） | GitHub 写操作回读比对 | `test_gh_verified_write.py` | 本地文件写入仍靠工具选择纪律 |
| 审查中加范围 | 复审材料带增量 diff；第三轮起强制「完整影响面」 | — | 扩张是否**必要**，由 Codex 按清单 F 节判 |
| 退出码契约被运行时破坏 | 策略脚本 0/1/2 分离；`trap` 归 2 | `MainExitCodeTests`（`main()` 对真实仓库跑 0/1/2 十条路径）、`ValidateCommitTests`、`Test-ReviewVerdict.ps1` | — |
| 审查中的 PR 里加范围（续） | 复审逻辑抽进 `lib\ReviewVerdict.ps1`（评论扫描、`reviewed-head` 提取、影响面节） | `Test-ReviewVerdict.ps1` 新增 25 用例 | `codex-review.ps1` 里剩下的 `gh` 集成路径仍无自动测试，靠 `-MaterialOnly` 手工验证 |
| 枚举代替整体比对 | 材料逐段比对 | `Compare-MaterialParts` 用例 | — |

> 右列不是遗憾，是分工：左边三列让审查者不必再花配额抓格式问题，右列才是独立审查真正该花时间的地方。

---

## 被驳回的意见

<!--
| 日期 | PR | 意见 | 为什么不改 |
| --- | --- | --- | --- |
-->

（暂无）

---

## 升级给人的分歧

| 日期 | PR | 分歧 | 结论 | 理由 |
| --- | --- | --- | --- | --- |
| 2026-09-10 | #2 | 仓库可见性与 spec §99「private」冲突。Claude 两次建议**升级 GitHub Pro 转回私有**（选项 A），理由是 `SPEC_REVIEW_v1.0.md` 是系统弱点清单、`REVIEW_FOLLOWUP_v1.1.md` 列了 5 条未解决残留，公开的代价高于每月 US$4 | **选 B**：接受公开，写 ADR + 修订 spec 至 v1.2 | 决策人不引入订阅成本。取舍与完整代价记录在 [ADR-0001](adr/ADR-0001-repository-visibility.md)，分歧本身也如实写进该 ADR 的「备选方案 A」 |

| 2026-09-11 | #22 | `GET /healthz` 要不要过设计闸门。Codex 主张「新增公共 API」就必须先过闸门，PR 写「不适用」是违反准入；Claude 主张 [WORKFLOW.md](WORKFLOW.md) §3 的分档表按**主题**分档，表里没有「API 改动」这一档，而返回恒定 `{"status":"ok"}`、无入参无鉴权无数据库的端点不落在任何受控主题里 | **选 B**：维持「不适用」，并把表格的空档补明确 | 按 A 做，Phase 0 剩下的 T0.2–T0.10 每个都要先开一个 11 节的闸门 Issue，正是 §3 自己警告的「敷衍填」。但分歧确实源于表里没写「既不碰钱、也不碰用量摄取 / 集成认证 / Webhook 的后端改动」归哪档——五处分档表各补一行，并加 `check_docs.py` 约定串规则钉住 |

| 2026-09-10 | #4 | nonce 重放存储该不该全部落库。Codex 主张五个签名端点全部落库，理由是「领域幂等属于业务层，不能折抵 §37 的认证层要求」；Claude 主张只有支付 Webhook 落库，理由是 §37 原文要求二者「结合」而非各自达标，且改钱的端点都已有永久唯一约束兜底 | **部分采纳**：支付 Webhook 落库，其余四个走 Redis | 支付 Webhook 是唯一真正来自外部的入口，量小、落库代价可忽略；其余四个要么有永久唯一约束，要么根本不改状态。明确记为一次风险接受，并写明重新评估的触发条件 |

> 处理方式参考：agent 的建议未被采纳时，**不要在后续对话里反复重提**。把理由和代价写进 ADR，让它成为可查的记录，然后按决策执行。
